import generatorsItems from 'ld-44/utils/generators/items';
import { module, test } from 'qunit';

module('Unit | Utility | generators/items', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = generatorsItems();
    assert.ok(result);
  });
});
