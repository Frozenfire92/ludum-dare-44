import dataSpells from 'ld-44/utils/data/spells';
import { module, test } from 'qunit';

module('Unit | Utility | data/spells', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = dataSpells();
    assert.ok(result);
  });
});
