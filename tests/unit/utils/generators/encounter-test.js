import generatorsEncounter from 'ld-44/utils/generators/encounter';
import { module, test } from 'qunit';

module('Unit | Utility | generators/encounter', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = generatorsEncounter();
    assert.ok(result);
  });
});
