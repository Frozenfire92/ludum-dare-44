import dataWeapons from 'ld-44/utils/data/weapons';
import { module, test } from 'qunit';

module('Unit | Utility | data/weapons', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = dataWeapons();
    assert.ok(result);
  });
});
