import Controller from '@ember/controller';
import { action } from '@ember/object';

export default class MapController extends Controller {
  @action explore() {
    console.log('TODO explore: story, battle, nothing');
    // Chance story
    // Chance battle
    // Chance nothing
  }
}
